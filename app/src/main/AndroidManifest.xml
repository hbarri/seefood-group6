<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.seefood">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SeeFood"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GalleryView"
            android:label="@string/title_activity_gallery_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ImageInflater"
            android:label="@string/title_activity_image_inflater"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ConfirmImages"
            android:label="@string/title_activity_confirm_images"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>